#include <stdio.h>

int main()
{
    float a;
    float b;
    printf("%f" , &a);
    if(a < 0)
    { 
        printf("errore");
    }
    if(a > 0)
    {
    b = 1.8 * a + 32;
    printf("in )
    }
}